package class5;

public class E1LogicalOperator {
    public static void main(String[] args) {
        //NOT(!) OR(||), AND(&&)
        //true=>false false=>true
        //(||) will help use combine multiple conditions in such a way
        //that as soon as one condition is true then the whole condition will be true

        boolean condition=false;// or u can do boolean condition=!false;
        //can also do condition=!condition;
        System.out.println(!condition);
    }
}
