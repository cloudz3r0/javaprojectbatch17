package review3;

public class E4SwitchCase {
    public static void main(String[] args) {
        //jsut like else if. can covert whenever you want to.
        //cant use logical so && or ||
        //cant use ==
        int age=10;
        switch(age) {
            case 10://to use logical operators. or just use switch with all operators
                if(age==10||age==20){

        }else if(age>90){

                }
                break;// need breaks between cases because it will print all true without checking
            case 20:

        }
    }
}
